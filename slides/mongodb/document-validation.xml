<slide>
<title>Enforcing Schema</title>

<blurb>Allows you to set a *validator*, a query that every new or updated document must match</blurb>
<blurb>Example:</blurb>
<example inline="1">
db.runCommand( {
	*collMod:* "whisky",
	*validator:* {
		name: { $type: "string" },
		age: { *$type:* "integer" },
	},
	validationLevel: *"moderate"*, // off, strict
	validationAction: *"warn"*, // error, 
} )
</example>
<break/>
	<blurb>MongoDB 3.6 adds JSON Schema support</blurb>
</slide>
